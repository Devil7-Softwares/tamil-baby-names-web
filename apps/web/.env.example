# Vite Configuration
# For overriding the dev server host
VITE_HOST=

# Google reCAPTCHA site key, inlined into the client bundle at BUILD time by
# Vite, so it must be set for `yarn build` / `yarn dev`.
RECAPTCHA_SITE_KEY=
